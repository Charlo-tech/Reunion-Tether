name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.2.2'
      SERVERPOD_PASSWORD_database: 'FuyXsWsdt2tMQ81IV0O767IDLJm0GcLp'
      SERVERPOD_PASSWORD_redis: 'yaAuzmQyMatBdgCBWmnTX1KqE2yhT9s3'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: reunion_tether_server
        run: docker compose up --build -d

      - name: Install dependencies
        working-directory: reunion_tether_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: reunion_tether_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: reunion_tether_server
        run: serverpod generate

      - name: Run tests
        working-directory: reunion_tether_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: reunion_tether_server
        run: docker compose down -v
