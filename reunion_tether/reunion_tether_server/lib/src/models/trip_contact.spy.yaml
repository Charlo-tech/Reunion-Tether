class: TripContact
table: trip_contact

fields:
  ### The trip ID
  tripId: int
  ### The contact ID
  contactId: int
  ### Whether visit is confirmed
  isConfirmed: bool
  ### Planned meetup date/time
  plannedMeetupDate: DateTime?
  ### Meetup location
  meetupLocation: String?
  ### Notes about this specific visit